{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "vercel-build": "echo 'Installing OpenSSL 1.0...' && yum install -y openssl10-libs && echo 'Running Composer...' && composer install --optimize-autoloader --no-dev && echo 'Running Laravel Optimizations...' && php artisan config:cache && php artisan route:cache && php artisan view:cache && echo 'Build finished.'"
    },
    "devDependencies": {
        "@tailwindcss/forms": "^0.5.2",
        "@tailwindcss/vite": "^4.0.0",
        "alpinejs": "^3.4.2",
        "autoprefixer": "^10.4.2",
        "axios": "^1.8.2",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.2.0",
        "postcss": "^8.4.31",
        "tailwindcss": "^3.1.0",
        "vite": "^6.2.4"
    }
}
